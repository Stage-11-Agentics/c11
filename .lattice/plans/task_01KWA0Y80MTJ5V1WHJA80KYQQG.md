# C11-156: OmpScraper cwd-slug-scoped walk (pi parity) — fix whole-tree ambiguity

Phase-4 Result Validator finding (GAP 1) on the exact-resume run. OmpScraper.candidates(cwd:) at merged main (cf177868b) calls filesystem.listSessionsRecursivelyByMtime(root, ...) UNCONDITIONALLY — the cwd param only stamps ScrapeCandidate.cwd, it never scopes the walk. So OmpScraper walks the entire ~/.omp/agent/sessions/ tree. With >1 omp session on disk across projects, restore hits the scrape-primary safe ambiguity-skip => omp does NOT resume. omp live validation passed only because it used a clean dedicated cwd (single resolvable candidate). This is the SAME whole-tree ambiguity bug PiScraper already fixed: PiScraper has a cwd-slug branch (sessionSlug(forCwd:) scopes the walk to ~/.pi/agent/sessions/<slug>/). The MV flagged this pre-validation (C11-154 ev_01KW8HW040RXS8RSYDK0NZDSYM) but the fix was NOT landed in the merged code despite the delegator/orchestrator recording it as fixed. FIX: port PiScraper cwd-slug scoping to OmpScraper — verify omp oh-my-pi cwd->slug encoding against omp own session-dir naming on disk (do NOT blindly copy pi encoding), add a test that with >1 cwd-slug dir on disk the current-cwd newest session resolves UNAMBIGUOUSLY. Scraper-only, reversible. Acceptance: omp resumes the exact session on a machine with multiple omp project dirs (multi-session live test), not just single-session. Strategy (OmpStrategy omp --resume=<id>) is already correct; this is scraper-only. Ref: validation-report.md GAP 1.
