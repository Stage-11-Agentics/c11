# C11-199: Is generic-metadata instantiation a real user-facing hang class? Judge from the post-classifier trend

C11-192 refuted its original premise (no c11-owned Button in any metadata-stack event; the 10s title came from Sentry grouping every hang into one issue) and deliberately did no view-tree work. This ticket owns the question the classifier makes answerable: once main-thread-hang/generic-metadata exists as its own Sentry issue, does it have meaningful distinct-user volume? Also compute the per-user spread of the pre-#401 generic-metadata captures (the 50-event bucket): if they came from the six users other than the 816-event outlier, this class is more widespread than its 5.7% suggests (adversarial-gemini's point from the C11-192 trident review). If the trend is real, open a fix ticket with a Time Profiler acceptance; if it stays near zero distinct users, close with that evidence. Note post-#401 throttling may starve the trend (see C11-192 review S7): state the observation window and the minimum event count for a verdict.
