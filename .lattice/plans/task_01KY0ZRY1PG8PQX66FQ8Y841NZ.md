# C11-180: c11 config CLI + config.* socket family

Design: docs/agent-config-primitive-design.md §6, §2.4 (stats), §5.6. Implement c11 config list/recent/stats/save/edit/rm/reorder/default/launch with --json variants exactly per §6 (incl. --follow-recent, --pin-current snapshotting recent into a saved config), reading/writing the state-root files app-down; socket config.* methods off-main per the threading policy; config launch as a thin client over agent.launch passing the recipe overlay, reusing AgentLaunchPlanner error codes, recording launch source=socket. Validation errors friendly (unknown name/id, invalid effort per harness template). HARD RULE: land the command family in skills/c11/SKILL.md + skills/c11/references/api.md and run scripts/sync-installed-skills.sh c11 in the same PR. Depends: C11-176, C11-177, C11-178 (T4/C11-179 for launch composition). AC: every §6 command with --json; app-down reads; stats windows; skill synced. CLI runtime is c11-logic-testable.
