Own review (orchestrator, inline ticket) of 796cf3eaa. Verdict: PASS. Scope: one new script, one new test, five one-line wrappings of existing xcodebuild calls, one CI step, docs. Checked: mkdir-atomic lock with no TOCTOU on acquisition; empty-pid window between mkdir and pid write is safe because takeover requires a present-and-dead pid; trap on EXIT/INT/TERM/HUP releases; SIGKILL leaves a stale lock that the next caller reclaims; exit code propagates via set -e; C11_BUILD_LOCK=0 exec-bypass keeps CI unaffected; reload.sh keeps its tee/PIPESTATUS shape (wrapper is PIPESTATUS[0]); test-unit*.sh keep exec. NIT: reloads.sh/reloadp.sh resolve the wrapper from $0 rather than a SCRIPT_DIR var, consistent with those scripts having none.