# C11-216: Move c11 builds off the laptop onto Atlas

Operator direction 2026-09-11: push all c11 builds to Atlas (Mac Studio, M2 Ultra, 24 cores, 128 GB, always-on at Fractal-Office, tailnet host 'atlas') and stop building on Hyperion at all. Readiness probe 2026-09-11 20:14 (Atlas local): macOS 26.5.2; repo checkout exists at /Users/atinwoodard/Projects/Stage11/code/c11; NO Xcode (xcode-select points at /Library/Developer/CommandLineTools; xcodebuild refuses to run); no zig; gh token cannot read private repos (c11 is public, ghostty fork is public, fine). PREREQUISITES (operator): install Xcode 26 on Atlas (App Store or 'xcodes install' with Apple ID), xcode-select -s to it, accept the license, install zig at the version ghostty's build.zig requires (or rely on scripts/download-prebuilt-ghosttykit.sh for GhosttyKit), and decide whether Release/notarized builds also move (needs Developer ID cert + notary keychain profile on Atlas). DESIGN OPTIONS to evaluate in the plan: (1) 'remote reload': scripts/reload.sh --host atlas rsyncs the worktree to atlas:~/c11-builds/<tag>/, runs the build there under the same build lock, and rsyncs the .app back and launches it locally (dev builds are launched on the laptop; the binary is arch-identical arm64); (2) a small build daemon/queue on Atlas fed over ssh (atlas-jobs pattern) with an artifact drop; (3) Xcode remote build via 'xcodebuild -destination' is not viable for macOS app targets. Also cover: DerivedData per tag on Atlas, GhosttyKit.xcframework cache there, per-worktree submodule provisioning, and the delegator boot prompts / c11-hotload skill switching the default to the remote path so no delegator ever runs xcodebuild on Hyperion. Depends on the local build lock ticket landing first (same lock semantics on Atlas).
